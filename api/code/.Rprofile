# ~/.Rprofile
# Prevent interactive prompts during package loading
options(ask = FALSE)

# All packages are pre-installed in Docker - disable runtime installations
options(install.packages.check.source = "no")
options(install.packages.compile.from.source = "never")
